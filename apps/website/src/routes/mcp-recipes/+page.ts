export const prerender = true;

export const load = () => ({
  pageMetaTags: {
    title: 'MCP recipes for PathWorks | Build courses with your agent',
    description:
      'Ready-to-run prompts for the PathWorks MCP server. Generate courses, refresh content from updated docs, tag launches, and more — from Cursor, Claude Code, Codex, or OpenCode.'
  }
});
